

Example datasets from Seaborn (`tips`, `penguins`, `diamonds`, etc.) or custom CSVs were used to demonstrate each visualization type.

---

## 🧰 Libraries Used

- `Seaborn` (main library)
- `Matplotlib` (for customization)
- `Pandas` (for data manipulation)

---

## 🗺️ Seaborn Plotting Roadmap

### ✅ 1. **Relational Plots**
- `scatterplot()` – axis-level
- `relplot()` – figure-level (supports multi-plot layouts)
- Parameters: `hue`, `size`, `style`
- Advanced: `FacetGrid`, `col_wrap`, `kind='line'`

### ✅ 2. **Distribution Plots**
- `histplot()` – histogram
- `distplot()` – older histogram (figure-level)
- `kdeplot()` – kernel density estimation
- `rugplot()` – tiny ticks for distribution
- Categorical histogram plotting
- Bivariate histograms and KDE plots

### ✅ 3. **Categorical Plots**
- `barplot()`
- `violinplot()`

### ✅ 4. **Regression Plots**
- `regplot()`
- `lmplot()` – figure-level with faceting

### ✅ 5. **Matrix Plots**
- `heatmap()` – with annotation, linewidths, color maps
- `clustermap()` – dendrogram-based grouping

### ✅ 6. **MultiPlots & Layouts**
- `FacetGrid`, `col`, `row`, `col_wrap`
- Grid-based multiplots using `relplot()` and `displot()`

---

## 🔍 Key Customizations Learned

- **Figure size control**
- **Using categorical variables for color & style**
- **Adding annotations to heatmaps**
- **Adjusting bin size in histograms**
- **Styling scatter/line plots by group**
- **Comparing axis-level vs figure-level functions**

---

## 📈 Sample Visuals Created

- 🎯 Scatter plot with hue, size, and style
- 📉 KDE plot + Rugplot + Bi-variate KDE
- 📊 Multi-histogram faceted by category
- 🔥 Annotated heatmap with custom color maps
- 🎛️ Violin vs Bar plots for categorical distributions
- 🔄 Clustermap showing grouping similarity

# 📊 Seaborn Categorical & Regression Plots - Visual Analytics with Python

This notebook is part of a complete **Seaborn Visualization Series**, focusing on mastering **categorical plots**, **regression analysis**, and **multi-plot grids** — core components for any data visualization and analytics project.

🔗 **GitHub Repository:** [Seaborn Visualizations](https://github.com/FizaAslam1/python-library/tree/main/seaborn)

---

## 🧭 Chapters Covered

### 📁 1. Categorical Plots

-  Introduction to Categorical Plots
- `stripplot()` – raw data spread with jitter
- `catplot()` – figure-level categorical plotting
- `swarmplot()` – spread with collision avoidance

### 📁 2. Categorical Distribution Plots

- *`boxplot()` – statistical summary using quartiles
-  `violinplot()` – box + KDE for distribution shape

### 📁 3. Categorical Estimate Plots

- * `barplot()` – central tendency with CI bars
- * `pointplot()` – point-wise trend comparison
- *`countplot()` – count of observations per category
- *Faceting with categorical plots (`row`, `col`, `hue`)

### 📁 4. Regression Plots

- `regplot()` – scatter + regression line (axis-level)
- `lmplot()` – regression with facets (figure-level)
-  `residplot()` – residual error plots

### 📁 5. Multi-Plot Grids

- `FacetGrid` – subplots for subgroups
- `pairplot()` & `PairGrid` – pairwise relationships
-  `JointGrid()` & `jointplot()` – bivariate distributions

### 🛠️ Bonus Utilities

- * `load_dataset()` – using built-in datasets like `tips`, `diamonds`, `penguins`
-  Blog idea generation for Seaborn

---

## 💡 Key Takeaways

- Difference between **axis-level** and **figure-level** functions
- How to visualize **categorical comparisons** with distribution and estimate plots
- Performing **linear regression** with multiple visual perspectives
- Leveraging **FacetGrid and multi-plot layouts** for better data insights
- Using Seaborn’s built-in datasets for practice and learning

---

## 🚀 How to Run

```bash
# Clone the repo
git clone https://github.com/FizaAslam1/python-library.git

# Navigate to Seaborn folder
cd python-library/seaborn

# Run in Jupyter
jupyter notebook

## 📁 Dataset

## 💼 Summary

This project demonstrates hands-on mastery of Seaborn’s powerful plotting system. From relational and distribution plots to statistical and matrix-level visuals — this repo reflects real-world skills for data analysis and storytelling in Python.

---

## 🌟 What's Next?

- Integrate Seaborn plots into **Jupyter Dashboards**
- Apply plots to real business data (Retail, Healthcare, etc.)
- Learn **Plotly** or **Altair** for interactivity

---

## 📌 Author

**Fiza Aslam**  


